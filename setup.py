from setuptools import setup, find_packages

setup(
    name='dsss_homework_2',
    version='0.1',
    author='Marian Horn',
    description='Homework 2 for DSSS course',
    packages=find_packages(),
    install_requires=[],
)

